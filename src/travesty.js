(function iifeTravesty() {
  'use strict';
  const TRACE = true;
  const DEBUG = true;
  const CHARS = "abcdefghijklmnopqrstuvwxyz '";

  if (TRACE) console.log('travesty.js loaded');
  
    function log(str) {
      if (DEBUG) console.log(str);
    }
    
    function transform(str, order) {
      let newstr = '';
      const arr = process(str);
      
      return str;
    }
    
    function process(str) {
      let arr = [];
      
      return arr;
    }
}());
